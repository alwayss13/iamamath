import React, { useState } from 'react';\nimport { View, Text, TextInput, Button } from 'react-native';\n\nconst Quiz = () => {\n    const questions = [\n        'Question 1: What is 1 + 1?',\n        'Question 2: What is 2 + 2?',\n        'Question 3: What is 3 + 3?',\n        'Question 4: What is 4 + 4?',\n        'Question 5: What is 5 + 5?'\n    ];\n    const [currentQuestionIndex, setCurrentQuestionIndex] = useState(0);\n    const [answers, setAnswers] = useState(Array(questions.length).fill(''));\n\n    const handleAnswerChange = (text) => {\n        const newAnswers = [...answers];\n        newAnswers[currentQuestionIndex] = text;\n        setAnswers(newAnswers);\n    };\n\n    const handleNextQuestion = () => {\n        setCurrentQuestionIndex((prevIndex) => Math.min(prevIndex + 1, questions.length - 1));\n    };\n\n    const handleSubmit = () => {\n        // Logic to grade answers can be added here\n        alert('Answers submitted!');\n    };\n\n    return (\n        <View>\n            <Text>{questions[currentQuestionIndex]}</Text>\n            <TextInput\n                keyboardType='numeric'\n                value={answers[currentQuestionIndex]}\n                onChangeText={handleAnswerChange}\n                placeholder='Enter your answer (1-5)'\n                maxLength={1}\n            />\n            <Button\n                title={currentQuestionIndex < questions.length - 1 ? 'Next Question' : 'Submit'}\n                onPress={currentQuestionIndex < questions.length - 1 ? handleNextQuestion : handleSubmit}\n            />\n        </View>\n    );\n};\n\nexport default Quiz;